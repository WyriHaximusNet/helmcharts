apiVersion: v1
kind: Pod
metadata:
  name: "{{ include ".helm.fullname" . }}-test-metrics-connection"
  labels:
{{ include ".helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include ".helm.fullname" . }}:7133']
  restartPolicy: Never
