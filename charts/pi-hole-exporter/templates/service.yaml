apiVersion: v1
kind: Service
metadata:
  name: {{ include ".helm.fullname" . }}
  labels:
    {{- include ".helm.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 9617
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include ".helm.selectorLabels" . | nindent 4 }}
